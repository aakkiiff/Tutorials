# Simple Ansible File Operations

This playbook demonstrates basic Ansible tasks:

* Create an empty file (`newfile.txt`)
* Create a file with content (`newfile2.txt`)
* Create a directory (`new_directory`)

Run the playbook:

```bash
ansible-playbook playbook.yml
```

All tasks run on `localhost`.
